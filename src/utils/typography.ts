import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  baseLineHeight: 1.666,
  headerFontFamily: ['IBM Plex Serif', 'source-han-serif-tc', 'serif'],
  bodyFontFamily: ['IBM Plex Serif', 'source-han-serif-tc', 'serif'],
});

export default typography;
